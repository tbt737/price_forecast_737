{
  "name": "cqp-api-worker",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker + Container wrapper for the FastAPI forecast service.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
